server:
  port: 18000
  servlet:
    context-path: /LncExpDB

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.118.215/lncexpression?serverTimezone=UTC&characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  thymeleaf:
    mode: HTML
    encoding: UTF-8
    cache: false
    servlet:
      content-type: text/html